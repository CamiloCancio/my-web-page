Vocabulario github

Branch: crear nueva rama
commit: son cambios realizados (se aceptan o no)

comandos git
git status: verifica el estado de lo eliminado o subido.
git rm: borra archivo deseado
git commit: hace un commit (para salir del modo se usa ZZ
git reset --soft head-1: deshacer un commit
git commit -m "arregla el diseño de la pagina web": para hacer un commit con comentario

git push origin: para que el commit se suba a la cuenta de git hub
actualizado 2.

code . (abrir el codigo)

push es para enviar datos

pull descarga datos
